import { useState, useEffect } from 'react';
import { createEarth } from '../components/gameHelpers';

export const useEarth = (player, resetPlayer) => {
    const [earth, setEarth] = useEarth(createEarth());

    
}